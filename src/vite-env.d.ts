// Injected by vite.config.ts's `define` — the real build fingerprint (git
// commit short hash + build timestamp), used to show which code a given
// deploy is actually running (see src/buildInfo.ts).
declare const __BUILD_COMMIT__: string;
declare const __BUILD_TIME__: string;
